for (let i=0; i < 5; i++) {
    console.log("Count:" + i); // Outputs numbers from 0 to 4
}

let fruits = ["apple", "banana", "cherry"];
for (let fruit of fruits) { 
    console.log(fruit); // Outputs each fruit in the array
}