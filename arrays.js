        let colors = ["red", "green", "blue", "yellow", "purple"];  // Initializing an array with color names
        colors.push("orange");                                      // Adding a new color to the end of the array
        colors.pop();                                               // Removing the last color from the array
        console.log(colors[0]);                                     // Outputs: red